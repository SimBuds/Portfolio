---
import type { SimpleIcon } from 'simple-icons';
import {
siDocker,
siGit,
siJavascript,
siTypescript,
siPython,
siReact,
siNextdotjs,
siNodedotjs,
siShopify,
siWordpress,
siContentful,
siMongodb,
siPostgresql,
siFigma
} from 'simple-icons';
import '../../assets/styles/TechStack.css';

type TechItem = { name: string; color: string; icon: SimpleIcon | null };

const techCategories: Record<string, TechItem[]> = {
  'Languages': [
    { name: 'JavaScript', color: '#F7DF1E', icon: siJavascript },
    { name: 'TypeScript', color: '#3178C6', icon: siTypescript },
    { name: 'Python', color: '#3776AB', icon: siPython }
  ],
  'Frameworks': [
    { name: 'React', color: '#61DAFB', icon: siReact },
    { name: 'Next.js', color: '#ffffff', icon: siNextdotjs },
    { name: 'Node.js', color: '#339933', icon: siNodedotjs }
  ],
  'CMS & E-Commerce': [
    { name: 'Shopify', color: '#96BF48', icon: siShopify },
    { name: 'WordPress', color: '#21759B', icon: siWordpress },
    { name: 'HubSpot', color: '#FF7A59', icon: null },
    { name: 'Contentful', color: '#2478CC', icon: siContentful }
  ],
  'Data & DevOps': [
    { name: 'MongoDB', color: '#47A248', icon: siMongodb },
    { name: 'PostgreSQL', color: '#336791', icon: siPostgresql },
    { name: 'Docker', color: '#2496ED', icon: siDocker },
    { name: 'GitHub Actions', color: '#2088FF', icon: null }
  ],
  'Cloud & Tools': [
    { name: 'AWS', color: '#FF9900', icon: null },
    { name: 'Azure', color: '#0078D4', icon: null },
    { name: 'Git', color: '#F05032', icon: siGit },
    { name: 'Figma', color: '#F24E1E', icon: siFigma }
  ]
};

---

<section class="tech-stack-banner">
  <div class="tech-stack">
    <div class="tech-stack-header">
      <h2>MY TECH STACK</h2>
      <p>
        The tools, languages, and platforms I use to design, build, and ship fast, reliable experiences.
      </p>
      <a href="/projectlist" class="tech-cta-button">
        View projects built with this stack
      </a>
    </div>

    <div class="tech-stack-categories">
      {Object.entries(techCategories).map(([category, items]) => (
        <div class="category">
          <h3>{category}</h3>
          <div class="tech-items">
            {items.map((tech: TechItem) => (
              <div class="tech-item">
                {tech.icon && (
                  <svg
                    class="tech-icon"
                    viewBox="0 0 24 24"
                    role="img"
                    aria-label={`${tech.name} icon`}
                    focusable="false"
                    style={`fill: ${tech.color}`}
                  >
                    <path d={tech.icon.path} />
                  </svg>
                )}
                <p>{tech.name}</p>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
